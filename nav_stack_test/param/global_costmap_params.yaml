global_costmap:
  global_frame: odom
  robot_base_frame: base_link
  update_frequency: 1
  publish_frequency: 1
  static_map: true
  rolling_window: false
  width: 10.0
  height: 10.0
  resolution: 0.05
#  static_map: true
# If you aren't using an existing map or map server, set the static_map parameter to false. 
# existing map or map server -> static_map: true

